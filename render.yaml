services:
  - type: web
    name: wb-parser
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    scaling:
      minInstances: 1
      maxInstances: 1
