services:
  - type: web
    name: wb-parser
    runtime: python
    buildCommand: "pip install -r requirements.txt && playwright install"
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12